// Discriminator enum for the Equipment family. Each value selects a
// variant subclass: Sword for Sword, Shield for Shield.

#nullable enable

namespace BleepforgeTestProject;

public enum EquipmentType
{
    Sword,
    Shield,
}
