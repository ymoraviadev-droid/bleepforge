// Enum keyed by ElementRegistry. Each value should have exactly one
// ElementData .tres in data/elements/<value>/<value>.tres.

#nullable enable

namespace BleepforgeTestProject;

public enum ElementKind
{
    Fire,
    Water,
    Earth,
    Air,
}
